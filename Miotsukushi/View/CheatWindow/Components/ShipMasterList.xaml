<UserControl x:Class="Miotsukushi.View.CheatWindow.Components.ShipMasterList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Miotsukushi.View.CheatWindow.Components"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid>
        <DataGrid IsReadOnly="True" ItemsSource="{Binding ShipList, Mode=OneWay}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="艦名" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="よみ" Binding="{Binding Yomi}"/>
                <DataGridTextColumn Header="艦種" Binding="{Binding ShipType}" SortMemberPath="ShipTypeID"/>
                <DataGridTextColumn Header="艦種ID" Binding="{Binding ShipTypeID}" Visibility="Hidden"/>
                <DataGridTextColumn Header="改造Lv" Binding="{Binding AfterShipLv}"/>
                <DataGridTextColumn Header="改造後" Binding="{Binding AfterShipName}"/>
                <DataGridTextColumn Header="リソースID" Binding="{Binding ResourceID}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
